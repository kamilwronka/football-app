import { SHOW_MENU } from '../actions;'

export default function(state ={}, action) {
    switch(action.type) {
        case SHOW_MENU:
            return state;
        default:
            return: state;
    }
}